# 🐧 Покрокове встановлення WSL2 для збірки APK

## Швидкий старт (автоматичне встановлення)

### Крок 1: Встановлення WSL
1. Клацніть правою кнопкою на `install_wsl.bat`
2. Виберіть **"Запустити від імені адміністратора"**
3. Дозвольте встановлення
4. **Перезавантажте комп'ютер** (обов'язково!)

### Крок 2: Перший запуск WSL
Після перезавантаження:
- WSL автоматично відкриється
- Створіть **логін** (латинськими літерами, без пробілів)
- Введіть **пароль** (двічі, при введенні символів не видно - це нормально)

### Крок 3: Налаштування середовища
1. Запустіть `setup_wsl_environment.bat` (звичайним способом, не як адміністратор)
2. Чекайте 5-10 хвилин - встановляться всі залежності
3. Готово! Тепер можна запускати `build_apk_simple.bat`

---

## Встановлення вручну (якщо автоматичне не спрацювало)

### Через PowerShell (як Адміністратор)

```powershell
# Встановлення WSL з Ubuntu
wsl --install

# Перезавантажте комп'ютер, потім:
wsl --status
wsl --list --verbose
```

### Перевірка версії WSL

```powershell
# Перевірити версію
wsl --status

# Якщо WSL1, оновіть до WSL2
wsl --set-version Ubuntu 2
wsl --set-default-version 2
```

---

## Налаштування WSL вручну

### Після першого запуску WSL:

```bash
# Оновлення системи
sudo apt update
sudo apt upgrade -y

# Встановлення залежностей для Buildozer
sudo apt install -y git zip unzip openjdk-17-jdk python3-pip \
  autoconf libtool pkg-config zlib1g-dev libncurses5-dev \
  libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev

# Встановлення Buildozer
pip3 install --user buildozer

# Додавання до PATH
echo 'export PATH=$PATH:~/.local/bin' >> ~/.bashrc
source ~/.bashrc

# Перевірка
buildozer --version
```

---

## Доступ до файлів Windows з WSL

Ваш проект на диску `D:\` доступний в WSL як:
```bash
/mnt/d/Python MY EXE/TelegramUserBot/telegram_user_bot_kivy
```

Або скопіюйте проект в WSL:
```bash
cp -r "/mnt/d/Python MY EXE/TelegramUserBot/telegram_user_bot_kivy" ~/
cd ~/telegram_user_bot_kivy
```

---

## Розв'язання проблем

### Помилка "WSL has no installed distributions"

**Рішення:**
```powershell
# Встановити Ubuntu вручну
wsl --install -d Ubuntu

# Або завантажити з Microsoft Store
```

### Помилка "WSL 2 requires an update"

**Рішення:**
1. Завантажте [WSL2 Linux kernel update package](https://aka.ms/wsl2kernel)
2. Встановіть пакет
3. Перезапустіть WSL: `wsl --shutdown`

### Помилка з правами адміністратора

**Рішення:**
- Завжди запускайте `install_wsl.bat` **як адміністратор**
- PowerShell адміністратор: клацніть правою → "Запуск від імені адміністратора"

### Повільна робота з файлами Windows

**Рішення:** Копіюйте проект в WSL файлову систему:
```bash
cp -r "/mnt/d/Python MY EXE/TelegramUserBot/telegram_user_bot_kivy" ~/
cd ~/telegram_user_bot_kivy
```

---

## Перевірка встановлення

### В Windows (PowerShell):
```powershell
wsl --status
wsl --list --verbose
```

### В WSL (bash):
```bash
# Перевірка версії Linux
cat /etc/os-release

# Перевірка Python
python3 --version

# Перевірка Buildozer
export PATH=$PATH:~/.local/bin
buildozer --version
```

---

## Готово!

Якщо все встановлено правильно:
1. ✅ WSL2 працює
2. ✅ Ubuntu встановлена
3. ✅ Buildozer встановлено
4. ✅ Залежності встановлені

**Тепер можна запускати `build_apk_simple.bat` для збірки APK!** 🚀

---

## Альтернативи WSL

Якщо WSL не підходить:
- **Віртуальна машина** з Ubuntu (VirtualBox/VMware)
- **Dual-boot** Linux
- **GitHub Actions** для автоматичної збірки (CI/CD)
- **Docker** (потребує налаштування)

---

## Додаткова інформація

- [Офіційна документація WSL](https://docs.microsoft.com/windows/wsl/)
- [Документація Buildozer](https://buildozer.readthedocs.io/)
- [Python-for-Android](https://github.com/kivy/python-for-android)



