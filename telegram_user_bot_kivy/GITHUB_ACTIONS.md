# 🚀 Автоматична збірка APK через GitHub Actions

## Як це працює

GitHub Actions автоматично збирає APK при кожному push в репозиторій або коли ви запускаєте workflow вручну.

## Налаштування

### 1. Створити GitHub репозиторій

```bash
# Якщо ще не створено репозиторій
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/TelegramUserBot.git
git push -u origin main
```

### 2. Перевірити що файли додані

Переконайтеся що в репозиторії є:
- `.github/workflows/build_apk.yml` ✅
- `telegram_user_bot_kivy/buildozer.spec` ✅
- `telegram_user_bot_kivy/main.py` ✅

### 3. Запустити збірку

**Автоматично:**
- Зробіть push в репозиторій
- GitHub Actions автоматично почне збірку

**Вручну:**
1. Перейдіть на GitHub в ваш репозиторій
2. Вкладка **Actions**
3. Виберіть **Build Android APK**
4. Натисніть **Run workflow**
5. Виберіть гілку (main/master)
6. Натисніть **Run workflow**

## Отримання APK

Після завершення збірки:

1. Перейдіть на вкладку **Actions**
2. Виберіть останній запуск workflow
3. Прокрутіть до секції **Artifacts**
4. Натисніть **telegram-user-bot-apk**
5. Завантажте APK файл

## Переваги

✅ **Не потрібен локальний Linux** - все збирається на сервері GitHub  
✅ **Автоматично** - при кожному push  
✅ **Безкоштовно** - для публічних репозиторіїв  
✅ **Чисте середовище** - Ubuntu з правильними залежностями  
✅ **Історія збірок** - всі APK зберігаються 30 днів  

## Налаштування

### Змінити версію APK

Відредагуйте `buildozer.spec`:
```ini
version = 1.0  # Змініть на потрібну версію
```

### Змінити архітектури

В `buildozer.spec`:
```ini
android.archs = arm64-v8a  # Тільки 64-bit (швидше)
# або
android.archs = arm64-v8a, armeabi-v7a  # Обидві (повільніше)
```

### Запускати тільки вручну

В `.github/workflows/build_apk.yml` видаліть:
```yaml
on:
  push:
    ...
  pull_request:
    ...
```
Залиште тільки:
```yaml
on:
  workflow_dispatch:
```

## Усунення проблем

### Збірка не запускається

- Перевірте що файл `.github/workflows/build_apk.yml` є в репозиторії
- Перевірте синтаксис YAML (можна на сайті GitHub)

### Помилка збірки

- Перевірте логи в GitHub Actions
- Переконайтеся що всі файли проекту закомічені
- Перевірте `buildozer.spec` на помилки

### APK не з'являється в Artifacts

- Перевірте що збірка завершилася успішно (зелена галочка)
- Перевірте логи на помилки
- Переконайтеся що APK файл створився в `bin/`

## Альтернатива: Збірка тільки для тегів

Якщо хочете збирати APK тільки при створенні тегу (релізу):

```yaml
on:
  push:
    tags:
      - 'v*'  # Збирати при тегах v1.0, v1.1, тощо
```

Тоді для збірки:
```bash
git tag v1.0
git push origin v1.0
```

---

**Готово!** Тепер просто робіть `git push` і APK буде збиратися автоматично! 🎉

